// Supabase public browser configuration.
// Replace these two placeholder values after creating the Supabase project.
// The anon key is intended to be public; database access is restricted by the SQL functions in supabase/setup.sql.
window.SWAP_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_PROJECT_URL",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY"
};
